// Public surface for the layout config layer.

export type { LayoutConfig, SpacingConfig, PackerConfig, ZoneLayoutConfig } from "./types.js";
export { resolveConfig, buildGlobalDefaults } from "./resolve_config.js";
