/**
 * LEGACY FACADE: scheduled for deletion post-M6.
 *
 * This is a re-export facade for generated/inventory_data.
 * Inventory is out of scope for the scene runtime activation plan.
 * It exists only to avoid breaking imports during the transition to the new runtime.
 * Future WPs will eliminate this facade and import directly from generated/inventory_data if needed.
 */

export {};
