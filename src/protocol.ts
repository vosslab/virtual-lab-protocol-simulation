/**
 * LEGACY FACADE: scheduled for deletion post-M6.
 *
 * This is a re-export facade for generated/protocol_data.
 * It exists only to avoid breaking imports during the transition to the new runtime.
 * Future WPs will eliminate this facade and import directly from generated/protocol_data.
 */

export {};
