object_name: test_missing_svg_target
kind: equipment
label: Test Missing SVG Target

state_fields:
  - field_name: active
    type: bool
    default: false
    description: Whether the test object is active. Used to trigger a visual state lookup.

visual_states:
  active:
    kind: svg
    cases:
      - when: false
        output: { asset_name: test_nonexistent_svg_asset_for_smoke }
      - when: true
        output: { asset_name: test_nonexistent_svg_asset_for_smoke }

capabilities: [clickable]

layout:
  default_width: 10
  label_width: 8
