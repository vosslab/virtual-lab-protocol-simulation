# Patch 0 baseline: 2026-05-08

> **Status: historical baseline before WP-H1.** Not current after later fixes.
> The 3/25 walker count is a historical observation; do not use it as a future
> metric. The walker ceiling is now tracked by step id, not by count.

Read-only baseline run before any trace or fix. No source edits.

## Gate results

| Gate               | Command                                                    | Result                           |
| ------------------ | ---------------------------------------------------------- | -------------------------------- |
| Build + bundle     | `bash build_github_pages.sh`                               | green; `dist/main.js` 584.6 KB   |
| Pytest             | `pytest tests/`                                            | 267 passed in 1.06s              |
| TypeScript         | `npx tsc --noEmit -p src/tsconfig.json`                    | clean (exit 0)                   |
| Resolver unit      | `node tests/playwright/test_interaction_resolver.mjs`      | 9/9                              |
| Index unit         | `node tests/playwright/test_interaction_index.mjs`         | 5/5                              |
| Coverage policy    | `node tests/playwright/test_completion_event_coverage.mjs` | 7/7                              |
| Graph reachability | `node tests/playwright/protocol_graph_smoke.mjs`           | 25/25                            |
| Game UI smoke      | `node tests/playwright/e2e/test_game_ui.mjs`               | 9/9                              |
| YAML walker        | `node tests/playwright/e2e/protocol_walkthrough_yaml.mjs`  | **3/25, fails at `add_trypsin`** |

## Walker failure detail

- Last step passed: `pbs_wash` (interactionSequence, 2 interactions, all four
  clicks `progressed` cleanly).
- Failing step: `add_trypsin` (interactionSequence, 2 interactions). Walker's
  four clicks all log `progressed` (serological_pipette, trypsin_bottle,
  serological_pipette, flask) but the step never completes. Failure mode:
  `page.waitForFunction: Timeout 30000ms exceeded`.
- Report path: `test-results/walker/playthrough_report.json`.

## Comparison to handoff observations

The handoff docs reported 264 pytest at pause; current run is 267 (3 new tests
landed in the working tree, expected). All other counts match the handoff
exactly. Walker failure point is identical.

## Conclusion

The dispatch asymmetry described in `docs/HANDOFF_CURRENT_STATUS.md` reproduces
on a fresh build. No stale-artifact issue. Proceeding to WP-T1 (trace) per the
plan.
