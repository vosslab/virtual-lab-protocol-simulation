# SVG Normalizer v3 wild-corpus verdicts

Generated by `tests/e2e/e2e_normalize_svg_wild.py` (WP-1c).

**This report reflects the FINAL normalizer (WP-1a through WP-3e plus gradient/visibility fix).**
It supersedes the earlier inflated count (2434 normalized) which was generated before WP-3b,
WP-3e, and the gradient/visibility fix added additional rejection gates. The true normalized
count is 721 of 3125 files (23.1%).

## Summary

- Total files: 3125
- Normalized: 721
- Rejected: 2404
- Unclassified crashes: 0
- Elapsed: 17.3s

## Rejections by reason code

| Reason code | Count |
| --- | --- |
| `CLIPPATH_UNSUPPORTED_COMPLEX` | 1120 |
| `STYLE_GEOMETRY_UNSUPPORTED` | 482 |
| `TEXT_UNSUPPORTED` | 295 |
| `DOCTYPE_OR_ENTITY` | 234 |
| `EMBEDDED_RASTER_UNSUPPORTED` | 73 |
| `USE_OR_SYMBOL_UNSUPPORTED` | 38 |
| `UNSUPPORTED_TRANSFORM` | 34 |
| `FILTER_UNSUPPORTED` | 32 |
| `MARKER_UNSUPPORTED` | 22 |
| `SCRIPT_OR_HANDLER` | 21 |
| `EMPTY_GEOMETRY` | 19 |
| `FOREIGNOBJECT_UNSUPPORTED` | 15 |
| `PARSER_ERROR` | 7 |
| `UNRESOLVED_REFERENCE` | 5 |
| `PATTERN_UNSUPPORTED` | 2 |
| `MASK_UNSUPPORTED` | 2 |
| `EXTERNAL_RESOURCE_UNSUPPORTED` | 2 |
| `UNSUPPORTED_UNIT` | 1 |

## Ref-integrity on normalized outputs

Every NORMALIZED output passes the ref-integrity gate (S1) by construction: the
`check_reference_integrity` step is a hard gate inside `normalize_svg_file`. A file
that passes S1 references only declared defs; any file with unresolved references is
rejected with `UNRESOLVED_REFERENCE` (5 files in this run) rather than producing a
broken normalized output. Zero normalized outputs have dangling references.

## Attribution spot-check (cc-by-3.0 / cc-by-4.0 bioicons)

Sample: 1 cc-by-4.0 DBCLS file and 10 cc-0 bioicons (Ewa-Oz/EwaOz/Qara/Gideon-Bergheim) with
pre-root XML comments were verified. Method: run v3 on each file; compare presence of
`rdf:RDF`, `dc:title`, `dc:format`, `cc:Work`, `creativecommons.org` markers before and
after; also verify pre-root XML comments survive.

Results:

- cc-by-3.0 (Servier, 0 checked): The Servier files carry only the cc/rdf/dc namespace
  prefix declarations (`xmlns:cc="..."`) with no actual license URL or title in their RDF
  blocks. The namespace prefixes are preserved by v3 via `lxml.etree` registered-prefix
  handling (same as v2). No attribution content is present to lose.
- cc-by-4.0 (1 file checked): `OTHER_REPOS/bioicons/static/icons/cc-by-4.0/Animals/DBCLS/Rhesus_monkey.svg`
  contains an rdf:RDF block with cc:Work and dc:title. All markers preserved after v3. **PASS**
- Pre-root comments (10 files checked): all 10 pre-root `<!-- ... -->` comment blocks
  survived normalization unchanged. **PASS (10/10)**

Attribution is preserved by v3's S4 serialization via lxml registered namespaces (dc, cc, rdf,
xlink, sodipodi, inkscape) and the `extract_pre_root_comments` / re-injection path. This
mirrors the v2 attribution-preservation guarantee and is confirmed by this spot-check.

## Next steps

The rejection counts above identify which feature classes to invest in next. A rejection
class graduates from reject to normalize only when a tested implementation is added to v3.
The gate is never loosened to pass ambiguous files.

Top candidates by count: `CLIPPATH_UNSUPPORTED_COMPLEX` (1120), `STYLE_GEOMETRY_UNSUPPORTED`
(482), `TEXT_UNSUPPORTED` (295), `DOCTYPE_OR_ENTITY` (234).
