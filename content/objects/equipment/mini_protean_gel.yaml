object_name: mini_protean_gel
kind: equipment
label: Mini-PROTEAN gel (sealed)

state_fields:
  - field_name: sealed
    type: bool
    default: true
    description: Whether the gel package is sealed.

visual_states:
  sealed:
    kind: svg
    cases:
      - when: true
        output: { asset_name: mini_protean_gel }
      - when: false
        output: { asset_name: mini_protean_gel }

capabilities: [clickable]

layout:
  default_width: 10
  display_width_cm: 18
  label_width: 8
