# Objects Quarantine

Objects held here are missing required SVG assets or have unresolved schema issues.
To restore an object: resolve the blocking issue, add the required SVG to assets/,
re-run validation, then move the object YAML back to content/objects/.
