# Base Scenes Quarantine

Scenes held here have missing object dependencies (objects not yet in content/objects/).
To restore a scene: ensure all required objects are present and valid, then move the
scene YAML back to content/base_scenes/ and re-run pipeline/gen_scene_index.py.
